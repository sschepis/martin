import { Adapter, ProductionManifest, Shot } from '../types';
export declare class SoraAdapter implements Adapter {
    name: string;
    generatePrompt(manifest: ProductionManifest, shot: Shot): string;
}
//# sourceMappingURL=sora.d.ts.map